<table id="dataTable"  class = "table table-bordered table-striped">
  <tr>
    <th>Email Subject</th>
    <th>Email Text</th>
    <th>Short Url</th>
   </tr>


  <% @referrals.each do |referral| %>
      <tr>

        <td><%= referral.email_subject %></td>
        <td><%= referral.email_text %></td>
        <td><%= referral.short_url  %></td>

        <td><%= link_to 'edit', {:action => 'edit', :id => offer} %></td>


        <td> <%= link_to 'delete', {:action=>'destroy', :id=> offer}, :confirm=>"You sure?", :method=> :delete %></td>



      </tr>

  <% end %>

</table>
