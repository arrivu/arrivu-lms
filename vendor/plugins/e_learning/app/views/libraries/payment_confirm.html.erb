<%
   @body_classes << "gradebook2 full-width"
   @show_left_side = false

%>
 <h3 style="text-align: center;">Order Details</h3>
<table class="table table-bordered table-striped">
<thead><th></th><th>Course Name</th><th>Description</th><th>Quantity</th><th>Price</th></thead>
  <tr ><td width="20%" style="text-align: center;">
    <% if @context_image_url %>
        <%= image_tag @context_image_url  , :style=>"width:100px;height:100px;"%>
    <% end  %>
  </td>
    <td><%= @context.name%></td><td><%= @context.course_description.try(:short_description)%></td><td>1</td><td><%= number_to_currency(@course_pricing.price.to_i, :precision => 2,:delimiter => ',')%></td></tr>
  <tr><td colspan="5" style="text-align: center;">
  <% @grouped_payments.each do |payments| %>
        <% payment = payments.first %>
        <%= content_tag :section, :class => :redirect do %>
            <div class="boxes">
              <%= render payments %>
            </div>
        <% end %>
    <% end %></td></tr>
</table>

