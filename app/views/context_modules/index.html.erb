
<% jammit_css :context_modules2,:flip_classes %>
<%
 if @context.feature_enabled?(:flipped_classes)
   content_for :page_title, join_title(t('titles.course_classes', "Course classes"), @context.name)
 else
   content_for :page_title, join_title(t('titles.course_modules', "Course modules"), @context.name)
 end
%>
<%= render :partial => "context_modules/content" %>


