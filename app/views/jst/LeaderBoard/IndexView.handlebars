
<div>
<label >{{course.name}}</label>
 {{#if courseSections.length}}

    <label style="margin-left: 27px;" >|</label>
  <label  for="live_class_sections_for_filter" style="padding: 34px;margin-top: -25px;">Sections</label>
    <select name="course_section_id" id="leader_board_sections_for_filter">
        <option value="0">All</option>
        {{#each courseSections}}
            <option value="{{id}}" id={{id}} >{{name}}</option>
        {{/each}}
    </select>
  {{/if}}
</div>


<div data-view="LeaderBoard"></div>

