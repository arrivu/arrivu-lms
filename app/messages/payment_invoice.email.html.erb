<% define_content :link do %>
    <%= HostUrl.protocol %>://<%= HostUrl.context_host(asset.account) %>/<%= asset.account.class.to_s.downcase.pluralize %>/<%= asset.account_id %>/subscriptions
<% end %>
<% define_content :subject do %>
  <%= t :subject, "Invoice Payment Confirmation" %>
<% end %>
<% email = asset.user.email; login = (asset.user.pseudonym.unique_id rescue "none") %>

<p>
  <%= "Dear Customer,"%>
</p>

<p>
  Your Payment of <b> &#8377; <%= Money.new(asset.transaction_amount.to_i, "INR")  %> </b> to Arrivu LMS for Transaction ID <b> <%= asset.merchant_transaction_id %> </b> is SUCCESSFUL.
</p>


 <b>ORDER DETAILS</b> <br/>
<table style="color: #666;
        font-size: 16px;
        text-shadow: 1px 1px 0px #fff;
        background: #eaebec;
        margin: 20px;
        border: #ccc 1px solid;
        -moz-border-radius: 3px;
        -webkit-border-radius: 3px;
        border-radius: 3px;
        -moz-box-shadow: 0 1px 2px #d1d1d1;
        -webkit-box-shadow: 0 1px 2px #d1d1d1;
        box-shadow: 0 1px 2px #d1d1d1;">
  <tr>
  <td>Merchant &nbsp;:</td>
    <td><%= HostUrl.protocol %>://<%= HostUrl.context_host(asset.account) %></td>
  </tr>
<tr>
  <td>Date &nbsp;:</td>
  <td> <%=asset.updated_at  %></td>
</tr>
<tr>
  <td>Amount &nbsp;:</td>
  <td>&#8377; <%= Money.new(asset.transaction_amount.to_i, "INR")  %></td>
</tr>
  <tr>
    <td>Transaction ID &nbsp;:	 </td>
    <td><%= asset.merchant_transaction_id %></td>
  </tr>

</table>



<p><a href="<%= content(:link) %>"><%= t(:link, "Click here to view your subscription details") %></a></p>


